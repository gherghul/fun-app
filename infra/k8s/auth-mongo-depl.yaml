apiVersion: apps/v1
kind: Deployment
metadata:
  name: fun-auth-mongo-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fun-auth-mongo
  template:
    metadata:
      labels:
        app: fun-auth-mongo
    spec:
      containers:
        - name: fun-auth-mongo
          image: mongo
---
apiVersion: v1
kind: Service
metadata:
  name: fun-auth-mongo-srv
spec:
  selector:
    app: fun-auth-mongo
  ports:
    - name: db
      protocol: TCP
      port: 27017
      targetPort: 27017
